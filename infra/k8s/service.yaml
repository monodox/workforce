apiVersion: v1
kind: Service
metadata:
  name: workforce-agents
  namespace: workforce
spec:
  type: ClusterIP
  selector:
    app: workforce-agents
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
---
apiVersion: v1
kind: Namespace
metadata:
  name: workforce
